#include <iostream>
using namespace std;

int Find_letter_pos(const char* str, char c)
{
  for (size_t i = 0; size < strlen(str); ++i)
  {
    if (str[i] == c)
      return pos;
  }
  return -1; // ничего не говорит
}

